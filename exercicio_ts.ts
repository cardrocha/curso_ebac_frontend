function multiplicar(numeroUm: number, numeroDois: number): number {
    return numeroUm * numeroDois;
}

function saudacao(nome: string): string { //fiz assim, foi a primeira forma que me veio na mente
    return `Olá + ${nome}`
}

function saudacao2(nome: string): string { //depois que li bem a proposta fiz assim, como esta escrito na descrição do exercício
    return "Olá" + nome;
}